[package]
name = "search_index_core"
version = "0.1.0"
description = "Private Rust search and normalization core embedded in ClipVault"
readme = "../../README.md"
publish = false
edition = "2024"
rust-version = "1.85"

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]

[lints.rust]
unsafe_code = "warn"

[lints.clippy]
all = "warn"
pedantic = "warn"
